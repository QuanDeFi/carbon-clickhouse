global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: carbon-local
    metrics_path: /metrics
    static_configs:
      - targets:
          - localhost:9464
        labels:
          pipeline_id: local-carbon
          decoder: local
          datasource: local
          mode: dev
