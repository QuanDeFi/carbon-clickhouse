//! This code was AUTOGENERATED using the Codama library.

use super::types::*;
#[allow(unused_imports)]
use carbon_core::clickhouse::rows::clickhouse_enum_variant;
use carbon_core::{
    clickhouse::{
        rows::{
            ClickHouseEventLandingMetadata, ClickHouseRowContext, CLICKHOUSE_EVENT_COMMON_COLUMNS,
        },
        ClickHouseColumnSpec,
    },
    instruction::InstructionMetadata,
};

#[derive(Debug, Clone, serde::Serialize)]
pub struct CandidateSwapResultsEventClickHouseRow {
    #[serde(flatten)]
    pub metadata: ClickHouseEventLandingMetadata,
    pub results: Vec<ClickHouseCandidateSwapResult>,
}

impl CandidateSwapResultsEventClickHouseRow {
    pub const FAMILY_NAME: &'static str = "jupiter_swap_candidate_swap_results";
    pub const EVENT_TYPE: &'static str = "candidate_swap_results";
    pub const DEFAULT_TABLE_NAME: &'static str = "jupiter_swap_candidate_swap_results_landing";
    pub const COMMON_COLUMNS: &'static [ClickHouseColumnSpec] = CLICKHOUSE_EVENT_COMMON_COLUMNS;
    pub const PAYLOAD_COLUMNS: &'static [ClickHouseColumnSpec] = &[ClickHouseColumnSpec::new(
        "results",
        CANDIDATE_SWAP_RESULT_ARRAY_DDL,
    )];

    pub fn from_parts(
        source: crate::events::candidate_swap_results::CandidateSwapResultsEvent,
        metadata: &InstructionMetadata,
        context: &ClickHouseRowContext,
    ) -> Self {
        let _ = &source;
        let event_seq = 0u32;

        Self {
            metadata: ClickHouseEventLandingMetadata::new(
                crate::PROGRAM_ID.as_ref(),
                crate::PROGRAM_ID.to_string(),
                Self::FAMILY_NAME,
                Self::EVENT_TYPE,
                event_seq,
                metadata,
                context,
            ),
            results: source
                .results
                .iter()
                .map(|value| ClickHouseCandidateSwapResult::from(value))
                .collect(),
        }
    }
}

carbon_core::impl_clickhouse_event_row!(
    CandidateSwapResultsEventClickHouseRow,
    super::clickhouse_event_table_options
);
