We start the Token Program example in a second terminal.

It uses the same database with a separate metrics port. First it writes a small USDC account snapshot, then it starts a live finalized block crawler for Token Program instructions.

Both example processes keep running in parallel.
