Finally, ClickHouse Play validates data.

The database menu shows landing tables with row and byte metadata, so we see what is populated.

We inspect Jupiter event and route rows, then Token Program transfer rows plus an account snapshot.

Jupiter shows swap legs; Token shows checked transfers and account authority state.

Table metadata plus decoded rows validates the pipeline end to end.
